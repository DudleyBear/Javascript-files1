let colorlist = ['gold', 'yellow', 'turquoise', 'red']

function setup() {
  createCanvas(windowWidth, windowHeight);
  background(52, 229, 235);
  //rectMode(CENTER);
  //rect(width/2,height/2,125,100)
  ellipse(400, 400, 300, 300);
}

//function draw() {
  //noStroke()
  //fill(random(colorlist));
  //ellipse(mouseX, mouseY, 25, 25);
//}
